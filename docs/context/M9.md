# M9 컨텍스트 스냅샷

> 생성: 2026-03-13 00:48

## 프로젝트 구조 (src/)

```
src/
├── __tests__/
│   ├── e2e.test.ts
│   └── pipeline.test.ts
├── agents/
│   ├── __tests__/
│   │   ├── developerAgent.test.ts
│   │   ├── plAgent.test.ts
│   │   ├── plannerAgent.test.ts
│   │   └── qaAgent.test.ts
│   ├── developerAgent.ts
│   ├── plAgent.ts
│   ├── plannerAgent.ts
│   ├── qaAgent.ts
│   └── prompts/
│       ├── __tests__/
│       │   └── prompts.test.ts
│       ├── developerPrompt.ts
│       ├── index.ts
│       ├── plannerPrompt.ts
│       ├── plPrompt.ts
│       └── qaPrompt.ts
├── index.ts
├── types/
│   └── index.ts
└── utils/
    ├── __tests__/
    │   ├── anthropicClient.test.ts
    │   ├── fileManager.test.ts
    │   ├── manifest.test.ts
    │   ├── responseParser.test.ts
    │   └── roundStateMachine.test.ts
    ├── anthropicClient.ts
    ├── fileManager.ts
    ├── logger.ts
    ├── manifest.ts
    ├── responseParser.ts
    └── roundStateMachine.ts
```

## 이번 마일스톤 변경 파일

- `src/utils/responseParser.ts` — 신규. JSON 단계적 복구 (fence 추출, trailing comma 제거, 스택 기반 truncation repair) + 필수 필드 런타임 검증 (`parseAndValidate`, `safeParseJson`, `validateFields`). 커스텀 에러: `ResponseParseError`, `ValidationError`
- `src/utils/anthropicClient.ts` — 수정. AbortController 타임아웃 (per-call + global), `AgentCallError` 에러 래핑 (network/api/timeout/unknown 분류), `abortAllRequests()`/`getGlobalAbortSignal()` 추가, `resetClient()`에 global abort 리셋 추가
- `src/agents/plAgent.ts` — 수정. `parseAndValidate` 적용 (RoundSpec 필수 6필드), `generateFailReport` 구조화된 박스 포맷으로 변경
- `src/agents/plannerAgent.ts` — 수정. `parseAndValidate` 적용 (FeatureBreakdown 필수 3필드)
- `src/agents/developerAgent.ts` — 수정. `parseAndValidate` 적용 (DevResult 필수 4필드)
- `src/agents/qaAgent.ts` — 수정. `parseAndValidate` 적용 (QAResult 필수 4필드)
- `src/index.ts` — 수정. graceful shutdown (SIGINT → `abortAllRequests()`, 2회째 강제 종료), `AgentCallError` 분류 처리 (abort 시 exit 130)
- `src/utils/parseJson.ts` — 삭제. `responseParser.ts`로 대체
- `src/__tests__/e2e.test.ts` — 신규. E2E 테스트 3종 (단순 게임 PASS, 강제 실패 maxRetries=0, retry 시나리오). `ANTHROPIC_API_KEY` 없으면 skip
- `src/utils/__tests__/responseParser.test.ts` — 신규. 21건 (정상 JSON, fence, 앞뒤 텍스트, trailing comma, truncation, validateFields, parseAndValidate)
- `src/utils/__tests__/anthropicClient.test.ts` — 수정. AbortController signal 전달 + AgentCallError 래핑 테스트 6건 추가 (총 20건)
- `src/agents/__tests__/*.test.ts` + `src/__tests__/pipeline.test.ts` — 수정. mock 경로 `parseJson.js` → `responseParser.js`, `parseJsonResponse` → `parseAndValidate`
- `README.md` — 수정. 기술 아키텍처 문서화 (에이전트 파이프라인, 데이터 계약, 상태 머신, 주요 기술적 결정)
- `package.json` — 수정. `test:e2e` 스크립트 추가, `test`에서 e2e 제외

## 아키텍처 상태

### 동작하는 것
- TypeScript strict mode + ESM (NodeNext) 빌드 파이프라인
- `npm run typecheck` → 에러 0
- `npm run build` → `dist/` 정상 생성
- `npm test` → 141 tests 전체 PASS (11 test files)
- 공유 타입 정의 (M1): 리터럴 유니온 4개, 에이전트 간 전달 인터페이스 8개, 상태/설정 4개
- 유틸리티 (M2): logger, fileManager, manifest, anthropicClient (3회 재시도 + exponential backoff, streaming, AbortController 타임아웃, AgentCallError 에러 분류)
- 상태 머신 (M3): createRoundState, transition, canRetry, incrementRetry, 데이터 슬롯 setter — 불변 상태 관리
- 에이전트 프롬프트 (M4): PL, Planner, Developer, QA 4종 system prompt + barrel export
- PL 에이전트 (M5): `runPLInit` — RoundSpec 생성
- Planner 에이전트 (M5): `runPlanner` — FeatureBreakdown 생성
- Developer 에이전트 (M6): `runDeveloper` — tool_use 루프, write/read/delete_file, manifest 즉시 갱신
- QA 에이전트 (M7): `runQA` — manifest 무결성 검증 + 파일 내용 수집 + AC 판정
- PL 판단 함수 (M7): `evaluateQAResult` (RELEASE/RETRY/FAIL), `generateFailReport` (구조화 박스 포맷)
- 전체 파이프라인 루프 (M7): stdin → PL_INIT → PLANNER_DEFINE → DEV_IMPLEMENT → QA_REVIEW → PASS→RELEASE→DONE / REJECT→RETRY→DEV_IMPLEMENT 재진입 / REJECT→FAIL→FAILED
- 에이전트 단위 테스트 (M8): PL 7건, Planner 3건, Developer 7건, QA 5건 — API mock 기반
- 파이프라인 통합 테스트 (M8): 데이터 계약 검증 6건 — 실제 roundStateMachine + 에이전트 mock
- JSON 응답 파서 (M9): `responseParser` — 단계적 복구 (fence 추출, brace 추출, trailing comma 제거, 스택 기반 truncation repair) + `validateFields` 필수 필드 런타임 검증
- AbortController 타임아웃 (M9): per-call + global abort, 에러 분류 (network/api/timeout/unknown)
- Graceful shutdown (M9): SIGINT → global abort → 두 번째 SIGINT → 강제 종료
- E2E 테스트 (M9): 3종 시나리오 (단순 PASS, 강제 FAIL, retry)
- README 완성 (M9): 기술 아키텍처, 데이터 계약, 주요 기술적 결정

### 아직 없는 것
- M9가 마지막 마일스톤이므로 추가 구현 사항 없음
- 향후 확장 가능: 멀티 라운드 (Round 2+), MCP 기반 git commit 자동화, 웹 UI

## 다음 마일스톤

M9가 마지막 마일스톤이다. 전체 마일스톤(M0~M9) 완료.
