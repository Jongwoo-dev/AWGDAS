# M1 컨텍스트 스냅샷

> 생성: 2026-03-10 14:00

## 프로젝트 구조 (src/)

```
src/
├── index.ts
└── types/
    └── index.ts
```

## 이번 마일스톤 변경 파일

- `src/types/index.ts` — 신규 생성 (에이전트 간 데이터 전달 인터페이스 14개)
- `docs/milestones/M1-types.md` — 수정 (마일스톤 문서 갱신)

## 아키텍처 상태

### 동작하는 것
- TypeScript strict mode + ESM (NodeNext) 빌드 파이프라인
- `npm run typecheck` → 에러 0
- `npm run build` → `dist/` 정상 생성
- 공유 타입 정의 완료:
  - 리터럴 유니온: `FileAction`, `FileRole`, `Verdict`, `RoundPhase`
  - 에이전트 간 전달: `AcceptanceCriteria`, `RoundSpec`, `Feature`, `FeatureBreakdown`, `ChangedFile`, `DevResult`, `CriteriaResult`, `QAResult`
  - 상태/설정: `RoundState`, `ManifestFile`, `Manifest`, `AgentConfig`

### 아직 없는 것
- 유틸리티 함수 (`src/utils/`)
- 상태 머신 로직
- 에이전트 프롬프트 (`src/agents/prompts/`)
- 에이전트 구현 (`src/agents/`)
- 테스트 설정 (vitest.config.ts)
- 실제 파이프라인 로직

## 다음 마일스톤

- **M2**: 유틸리티 + 테스트
- 핵심 목표: Anthropic SDK 래퍼, 파일 I/O, manifest 파싱 등 공통 유틸리티를 `src/utils/`에 구현하고 단위 테스트 작성
- 세부 문서: `docs/milestones/M2-utils.md`
