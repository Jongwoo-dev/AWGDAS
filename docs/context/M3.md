# M3 컨텍스트 스냅샷

> 생성: 2026-03-11 14:50

## 프로젝트 구조 (src/)

```
src/
├── index.ts
├── types/
│   └── index.ts
└── utils/
    ├── __tests__/
    │   ├── anthropicClient.test.ts
    │   ├── fileManager.test.ts
    │   ├── manifest.test.ts
    │   └── roundStateMachine.test.ts
    ├── anthropicClient.ts
    ├── fileManager.ts
    ├── logger.ts
    ├── manifest.ts
    └── roundStateMachine.ts
```

## 이번 마일스톤 변경 파일

- `src/utils/roundStateMachine.ts` — 신규 생성 (Round 상태 전환 로직, 불변 상태 관리)
- `src/utils/__tests__/roundStateMachine.test.ts` — 신규 생성 (23 tests)

## 아키텍처 상태

### 동작하는 것
- TypeScript strict mode + ESM (NodeNext) 빌드 파이프라인
- `npm run typecheck` → 에러 0
- `npm run build` → `dist/` 정상 생성
- `npm test` → 63 tests 전체 PASS
- 공유 타입 정의 (M1): 리터럴 유니온 4개, 에이전트 간 전달 인터페이스 8개, 상태/설정 4개
- 유틸리티 (M2):
  - `logger`: createLogger + child() 패턴, 로그 레벨 필터링 (AWGDAS_LOG_LEVEL)
  - `fileManager`: ensureOutputDir, writeGameFile, readGameFile, deleteGameFile, gameFileExists — path traversal 방지, baseDir DI
  - `manifest`: createManifest, readManifest, addFileToManifest, updateFileInManifest, removeFileFromManifest — 섹션 18 준수
  - `anthropicClient`: getClient (싱글톤), callAgent (텍스트 전용), callAgentWithTools (tool_use) — 3회 재시도 + exponential backoff (1s/2s/4s), role별 maxTokens/timeout
- 상태 머신 (M3):
  - `roundStateMachine`: createRoundState, transition (유효 전환 맵 기반), canRetry, incrementRetry, 데이터 슬롯 setter (setSpec, setBreakdown, setDevResult, setQAResult, addToBacklog)
  - 불변 상태 관리 (스프레드 연산자), RETRY_CHECK → DEV_IMPLEMENT 시 retry 한도 검증
  - 전환 맵: PL_INIT → PLANNER_DEFINE → DEV_IMPLEMENT → QA_REVIEW → RELEASE/RETRY_CHECK → DONE/FAILED
- 에이전트 설정: 기본 모델 claude-opus-4-6, PL 4096/30s, Planner 16384/60s, Developer 32768/120s, QA 16384/60s

### 아직 없는 것
- 에이전트 프롬프트 (`src/agents/prompts/`)
- 에이전트 구현 (`src/agents/`)
- 실제 파이프라인 로직 (`src/index.ts` — 현재 stub)
- JSON 응답 파서 (M9에서 추가 예정)

## 다음 마일스톤

- **M4**: 에이전트 프롬프트
- 핵심 목표: PL, Planner, Developer, QA 에이전트의 system prompt 정의
- 세부 문서: `docs/milestones/M4-prompts.md`

- **M5**: PL + Planner + 부분 파이프라인 (M2, M3, M4 의존)
- 핵심 목표: PL → Planner 연결, index.ts 부분 파이프라인 구성
- 세부 문서: `docs/milestones/M5-pl-planner.md`
