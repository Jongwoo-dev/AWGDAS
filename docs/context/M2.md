# M2 컨텍스트 스냅샷

> 생성: 2026-03-10 14:30

## 프로젝트 구조 (src/)

```
src/
├── index.ts
├── types/
│   └── index.ts
└── utils/
    ├── __tests__/
    │   ├── anthropicClient.test.ts
    │   ├── fileManager.test.ts
    │   └── manifest.test.ts
    ├── anthropicClient.ts
    ├── fileManager.ts
    ├── logger.ts
    └── manifest.ts
```

## 이번 마일스톤 변경 파일

- `vitest.config.ts` — 신규 생성 (vitest 테스트 설정)
- `docs/system-design.md` — 수정 (기본 모델 opus-4-6 변경, max_tokens 상향)
- `src/utils/logger.ts` — 신규 생성 (구조화된 로깅, phase/roundId/agent 컨텍스트)
- `src/utils/fileManager.ts` — 신규 생성 (파일 CRUD, path traversal 방지)
- `src/utils/manifest.ts` — 신규 생성 (manifest.json CRUD, 섹션 18 규칙 준수)
- `src/utils/anthropicClient.ts` — 신규 생성 (Anthropic SDK 래퍼, 재시도, role별 config)
- `src/utils/__tests__/fileManager.test.ts` — 신규 생성 (11 tests)
- `src/utils/__tests__/manifest.test.ts` — 신규 생성 (13 tests)
- `src/utils/__tests__/anthropicClient.test.ts` — 신규 생성 (16 tests)

## 아키텍처 상태

### 동작하는 것
- TypeScript strict mode + ESM (NodeNext) 빌드 파이프라인
- `npm run typecheck` → 에러 0
- `npm run build` → `dist/` 정상 생성
- `npm test` → 40 tests 전체 PASS
- 공유 타입 정의 (M1): 리터럴 유니온 4개, 에이전트 간 전달 인터페이스 8개, 상태/설정 4개
- 유틸리티:
  - `logger`: createLogger + child() 패턴, 로그 레벨 필터링 (AWGDAS_LOG_LEVEL)
  - `fileManager`: ensureOutputDir, writeGameFile, readGameFile, deleteGameFile, gameFileExists — path traversal 방지, baseDir DI
  - `manifest`: createManifest, readManifest, addFileToManifest, updateFileInManifest, removeFileFromManifest — 섹션 18 준수
  - `anthropicClient`: getClient (싱글톤), callAgent (텍스트 전용), callAgentWithTools (tool_use) — 3회 재시도 + exponential backoff (1s/2s/4s), role별 maxTokens/timeout
- 에이전트 설정: 기본 모델 claude-opus-4-6, PL 4096/30s, Planner 16384/60s, Developer 32768/120s, QA 16384/60s

### 아직 없는 것
- 상태 머신 로직 (`src/utils/` 또는 별도 모듈)
- 에이전트 프롬프트 (`src/agents/prompts/`)
- 에이전트 구현 (`src/agents/`)
- 실제 파이프라인 로직 (`src/index.ts` — 현재 stub)
- JSON 응답 파서 (M9에서 추가 예정)

## 다음 마일스톤

- **M3**: 상태 머신 + 테스트 (M1 의존, M2와 병렬 가능)
- 핵심 목표: Round 상태 머신 구현 — phase 전환 로직, Retry Policy 관리, 상태 전환 검증 테스트
- 세부 문서: `docs/milestones/M3-state-machine.md`

- **M4**: 에이전트 프롬프트 (M1 의존, M2와 병렬 가능)
- 핵심 목표: PL, Planner, Developer, QA 에이전트의 system prompt 정의
- 세부 문서: `docs/milestones/M4-prompts.md`
